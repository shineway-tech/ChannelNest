This directory contains the Kuaishou creator-platform `$encode` runtime used to
generate `__NS_sig3` for creator-center management APIs.

The current resource is extracted from the logged-in
`https://cp.kuaishou.com/article/manage/video` frontend bundle. Keep the file
unchanged unless the Kuaishou creator-platform signing module changes.

